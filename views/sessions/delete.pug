extends ./sessions-layout

block content
  h3 Delete Session

  p Are you sure you want to delete this session?

  form(action=session.urlDelete, method="POST")
    div.form-group.button-list
      button.btn.btn-danger(type='submit') Delete
      a.btn.btn-default(href=session.urlDetails) Cancel